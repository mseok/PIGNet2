# @package data
cross:
  key_dir: ${hydra:runtime.config_sources\[1\].path}/../keys/train/PDBbind_v2020/cross
  data_dir:
  processed_data_dir:
  label_file:
  loss_range: [0.0, null]
  loss_ratio: 5.0
  objective: augment
  train_result_path: train_cross.txt
  test_result_path: test_cross.txt
  pos_noise_std:
  pos_noise_max:
